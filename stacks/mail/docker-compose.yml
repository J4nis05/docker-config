# E-mail Server
version: "3.8"
name: "mail"


services:
  example:
    image: 
    container_name: 
    restart: always
    ports:
      - 
    volumes:
      - 
    environment:
      - 


networks:
  default:
    external: true
    name: proxynet


volumes:
  