# Use an official web server image as the base
FROM nginx:alpine

# Copy the static website files into the container
COPY ./data/cryotea.com /usr/share/nginx/html

# Expose the default HTTP port (80)
EXPOSE 80